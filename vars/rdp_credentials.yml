---
# =============================================================================
# RDP credentials for the ubuntu_desktop_rdp role.
# =============================================================================
#
# These values authenticate incoming RDP (Remote Login) sessions. Replace the
# placeholders before running the playbook.
#
# To keep the password out of plain text, encrypt this file:
#   ansible-vault encrypt vars/rdp_credentials.yml
# and run the playbook with --ask-vault-pass.
# =============================================================================

rdp_username: "CHANGEME"
rdp_password: "CHANGEME"
